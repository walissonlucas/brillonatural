<div class="pt-24 pb-16 min-h-screen flex items-center justify-center">
  <div class="container-custom text-center">
    <h1 class="font-poppins font-semibold text-4xl mb-4">404 Página no encontrada</h1>
    <p class="text-dark-light mb-8 max-w-lg mx-auto">Lo sentimos, la página que estás buscando no existe o ha sido movida.</p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a href="{{ routes.root_url }}" class="btn-primary">Volver al inicio</a>
      <a href="{{ routes.collections_url }}" class="btn-secondary">Ver productos</a>
    </div>
  </div>
</div>